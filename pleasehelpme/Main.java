import java.util.*;import java.io.*;public class Main {    public static class shop implements Comparable<shop>{        int x;        int y;                public shop(int x, int y) {            this.x = x;            this.y = y;        }                public int compareTo(shop other) {            return this.x-other.x;        }    }    public static void main(String[] args) throws IOException {        // TODO Auto-generated method stub        Scanner sc = new Scanner(System.in);        int N = sc.nextInt();        shop[] shops = new shop[N];                        for(int i = 0; i < N; i++) {            int x = sc.nextInt(), y = sc.nextInt();                        shops[i] = new shop(x, y);        }        Arrays.sort(shops);                        double ans = Math.sqrt((shops[0].x-0)*(shops[0].x-0) + (shops[0].y-0)*(shops[0].y-0));                for(int i = 0; i < N-1; i++) {            int x1 = shops[i].x;            int y1 = shops[i].y;            int x2 = shops[i+1].x;            int y2 = shops[i+1].y;                        ans += Math.sqrt((x2-x1)*(x2-x1) + (y2-y1)*(y2-y1)); //apply distance formula        }        ans += Math.sqrt((100-shops[N-1].x)*(100-shops[N-1].x) + (100-shops[N-1].y)*(100-shops[N-1].y));                System.out.println((int)ans);    }}