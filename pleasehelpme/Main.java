import java.util.*;public class Main{    public static void main(String[] args) {        Scanner input = new Scanner(System.in);        int friends = input.nextInt();                int imposters = input.nextInt();                int friend_distance;        int[] friend_num = new int[friends];        int imposter_distance;        int[] imposter_num = new int[imposters];                for(int i = 0; i<friends; i++) {                friend_distance = input.nextInt();                        friend_num[i] = friend_distance;        }        //System.out.println(Arrays.toString(friend_num));                for(int i = 0; i<imposters;i++) {                imposter_distance = input.nextInt();                            imposter_num[i] = imposter_distance;        }                            int count = 0;            for(int i = 0; i<imposters; i++) {    	boolean ans = false;        int low = 0;        int high = friends-1; //-1 because arrays are 0-indexed        int mid = (low + high)/2;                while(low <= high) { //if they converge that means it doesnt exist in the array         	        if(imposter_num[i]>friend_num[mid]) {            low = mid+1; //+1 because we can exclude the current one as well, it also makes the new while loop condition work                    mid = (low+high)/2; //previously these were (low+high/2);        }else if(imposter_num[i]<friend_num[mid]){            high = mid-1; //we can exclude the current one as well            mid = (low+high)/2;        }else {            ans = true;            break;            }        }                if(ans) System.out.println("yes");        else System.out.println("no");    }        }}